// import { searchTweet } from '@/service/search';

// export default function SearchResult() {
//   return (
//     <div>
//       <ul>
//         {results.length > 0
//           ? results.map((user: any) => <li key={user.id}>{user.username}</li>)
//           : query && <li>검색 결과가 없습니다.</li>}
//       </ul>
//     </div>
//   );
// }
